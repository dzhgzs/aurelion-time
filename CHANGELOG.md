# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)，格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [1.4.6] - 2026-09-22

### 新增
- **自绘软件更新弹窗**：升级提示由系统原生对话框改为与界面风格统一的深色金边弹窗（AURELION Software Update），覆盖「发现新版本 / 已是最新 / 检查失败（HTTP 异常 / 未配置升级源 / 网络不可达）」五种状态；发现新版可一键直达发布页下载，已是最新时约 2 秒自动关闭，支持遮罩点击与「✕」关闭。

### 修复
- **全屏退出状态丢失**：桌面端全屏切换改为原生窗口全屏（IPC `setFullScreen`，更稳定），进入前记录窗口状态，退出全屏后自动恢复进入前的最大化状态；进入 / 退出均同步全屏按钮高亮，浏览器模式回退 DOM Fullscreen。

### 优化
- **顶部信息栏重设计**：移除「GRANDE COMPLICATION · 自动上链机械 / 限量 88 枚 · ¥1,280,000」装饰文案，改为三行对齐展示：大号时分（31px）+ 金色秒、年月日 + 星期、农历 + 节日；渲染按字段独立去重，窄窗口自动缩小字号。

## [1.4.5] - 2026-09-22

### 新增
- **建议 / BUG 反馈入口（GitHub Issues）**：主界面底部新增「建议 / BUG 反馈」按钮、托盘菜单新增同项，一键直达 [Issues 版块](https://github.com/dzhgzs/aurelion-time/issues) 提交留言。桌面端经主进程白名单校验后调用系统浏览器打开；浏览器直开模式回退新标签页。反馈地址沿用 package.json `bugs` 字段。

## [1.4.4] - 2026-09-21

### 优化
- **设置区收展动画重做（丝滑化）**：`max-height: 0→360px` 魔法数字过渡改为 **`grid-template-rows: 0fr → 1fr` 按内容真实高度插值**——消除收起时前段无位移、尾段空跑的"顿挫感"；展开叠加 10px 上浮位移与分层延迟淡入；手柄悬停增加金色底色反馈，箭头缓动同步为同一曲线。
- **无障碍修复**：收起时设置内容改为 `visibility` 过渡隐藏——修复收起状态下隐藏按钮（导出/导入）仍可被 Tab 键聚焦的问题。

### 验证
- 帧采样：展开 0 → 79 → 198 → 254px 平滑插值；三档窗口布局断言（不覆盖页签/底部操作/不溢出面板）全部保持通过。

## [1.4.3] - 2026-09-21

### 新增
- **操作系统与运行环境说明书** `docs/SYSTEM-REQS.md`：OS 支持矩阵（Win10 1809+/Win11/macOS 10.15+/浏览器模式）、硬件与图形需求、系统权限与集成能力对照、数据存储路径、已知限制、故障排查与卸载说明。
- **README 重构为完整开源主页**：软件详细介绍、功能全景表、目录导航、下载安装、快速上手、FAQ（折叠式）、贡献与安全链接，符合 GitHub 社区规范。

### 调整
- 智能输入节日令牌补齐至 `festOf` 全量覆盖（新增 妇女节/植树节/青年节/建党节/建军节），并对齐注释防止后续失配。

## [1.4.2] - 2026-09-21

### 新增
- **设置区智能收起/展开**：底部「⚙ 设置 · 响铃 · 备份」默认收起为一条手柄，鼠标悬停自动弹出、移开自动收起（320ms 缓冲防误触）；单击手柄可**固定展开**（记忆偏好，托盘配置持久化）；键盘焦点进入自动展开并同步 `aria-expanded`；拖动音量滑杆途中不误收。功能内容区可用高度提升约 100px。

## [1.4.1] - 2026-09-21

### 修复
- **时间助手抽屉布局**：底部设置区过高，覆盖页签与功能内容区。根治方案：设置区改两列网格（高度 408px → 288px）、`max-height: calc(100% - 172px)` 上限 + 内部滚动、内容区保底 56px、头部/页签 `flex-shrink:0` 防压缩。实测 1200×800 / 1000×640 / 900×560 三档均不再溢出、不再覆盖。

## [1.4.0] - 2026-09-21

### 新增
- **节日时间解析**：智能输入「春节」「中秋节」「元旦」等节日名自动落到下一个该节日的公历日期（农历/公历/法定节日全支持，当天已过自动顺延至明年）。
- **闹钟快速添加**：15/30/60 分钟后、明早 7:30 一键创建一次性闹钟（`fireDate` 定日）。
- **贪睡状态持久化**：应用重启后贪睡不丢；已到点但错过 5 分钟内自动补响。
- **倒计时「+1 分」微调**：运行中延长当前段，空闲时预加时长（封顶 999 分钟）。
- **今日番茄统计**：专注循环显示「今日 N 🍅」，按天归零、跨会话累计。

### 修复
- 一次性闹钟在错过补响宽限窗内「当天误响」的问题（新设闹钟原会在当天也触发一次）。

### 无障碍
- 时间助手页签补全 `tablist/tab/tabpanel` 角色与 `aria-selected` 状态同步。

## [1.3.0] - 2026-09-21

### 新增
- **世界时钟页签**：八大城市（北京/东京/新加坡/悉尼/迪拜/伦敦/纽约/洛杉矶），按与本地时差自动排序、本地置顶，工作日 9–18 点标注「营业中」，附智能快慢换算。
- **循环倒计时**：结束后自动重开同一时长，显示已循环轮数。
- **专注循环自定义时长**：专注 5–180 分钟 + 休息 1–60 分钟，改动即生效于下一段。
- **重复闹钟智能路由**：智能输入「每天早上7点跑步」「每周三下午5点开会」自动创建重复闹钟。
- **任务快速添加**：+30 分钟 / +1 小时 / +2 小时 / 今晚 21:00 / 明早 8:00 一键加入；备注行内编辑；+1天 / +1时 快捷顺延。
- **响铃键盘操作**：`Esc`/回车 停止 · `空格`/`S` 贪睡，焦点自动直达停止按钮。
- **应用内升级接口**：读取 GitHub Releases 感知新版本（托盘「检查更新」+ 每 24 小时静默自检）。
- 法定假日 2027–2100 兜底：表未收录年份按法定基准日自动生成，界面标注「暂按法定假日，未含调休」。

### 修复
- 智能输入「1小时后」「1.5小时后」解析失败落入默认 9 点的错误。
- 已过去的日期/星期表达被拒绝：现自动顺延（周X→下周、X号→下月、X月X日→明年）。
- 托盘挂机后台定时器被节流导致闹钟迟到（禁用 backgroundThrottling）。
- 窗口缩放丢失性能自适应档位；软件渲染环境恒定锁定 1 倍缓冲。
- 整点报时秒级漂移漏响（改为「日期+小时」键控）。
- 打包版 crash.log 写入只读目录失败（改写 userData）。
- 任务「每年」标签重复显示；tkAdd 过去时间静默失败（改为明确提示）。
- 二次启动不还原最小化窗口。

### 安全 / 无障碍
- 显式启用渲染沙箱、拒绝 webview 注入；未捕获 Promise 异常上报诊断日志。
- 全部开关 aria-checked 同步、toast 播报区、`prefers-reduced-motion` 适配、刘海屏安全区、焦点描边全覆盖。
- 倒计时最后 10 秒预警夜间自动柔化。

## [1.2.2] - 2026-09-21

### 新增
- 绿色版形态（zip 解压一次毫秒级秒开）。
- 「先壳后内容」启动策略：加载遮罩立即可见，感知响应亚秒级。

### 修复
- 便携/安装/开发版缓存目录争用导致的启动缓慢（统一 userData + 单实例锁前置）。

## [1.2.1] - 2026-09-21

### 修复
- FPS 自适应在 dpr=1 设备上「降档反放大」导致的画布异常与卡死（档位单调不超 dpr，默认关闭）。
- 软件渲染（SwiftShader/llvmpipe）环境自动降载：关阴影/尘埃/毛玻璃。

### 新增
- 机芯零件 16 → 22 组：发条、擒纵叉（红宝石叉瓦）、避震器、立体时标、刻度圈、夜光点。

## [1.2.0] - 2026-09-21

### 新增
- 中文自然语言时间解析（30分钟后 / 明天下午3点 / 下周三晚上8点 / 3月8日…）。
- 渐强唤醒、时段问候语、番茄专注循环、语音报时、整点报时。
- 常用倒计时时长学习（高频置前标注「常用」）。
- 闹钟智能相对时间（「3 小时 20 分后响」）、toast 组件与首次引导。

## [1.1.0] - 2026-09-21

### 新增
- 自研农历/节假日引擎（1900–2100 全量数据含闰月，干支生肖，传统节日，清明节气公式）。
- 表冠调时模式：拖动/滚轮校准，跨午夜自动换日，表盘日历窗联动。
- Windows 便携版 / NSIS 安装版 / macOS zip 打包（electron-builder）。

## [1.0.0] - 2025-09-20

### 首个发布
- 程序化建模 3D 机械腕表（three.js r152，无外部模型，离线可运行）。
- 时间助手：闹钟 / 倒计时 / 定时任务 / 秒表 / 整点报时，托盘常驻，窗口置顶，开机自启。
- 持久化：localStorage + 主进程配置文件镜像双写。
